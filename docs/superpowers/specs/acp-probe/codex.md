# codex ACP probe

- Spawn: `npx -y @agentclientprotocol/codex-acp@1.7.0`
- Session disk: `~/.codex/sessions/<folder>` (name = id; no vendor JSON parse)
- Resume: `session/load` or `session/resume` on the codex child
- Initialize: see README default blob (live dump not captured)
