# kimi ACP probe

- Spawn: `kimi acp`
- Session disk: `~/.kimi-code/sessions/<folder>` (name = id; no vendor JSON parse)
- Resume: `session/load` or `session/resume` on the kimi child
- Initialize: see README default blob (live dump not captured)
