# claude ACP probe

- Spawn: `npx -y @agentclientprotocol/claude-agent-acp@0.70.0`
- Session disk: `~/.claude/projects/<folder>` (name = id; no vendor JSON parse)
- Resume: `session/load` or `session/resume` on the claude child
- Initialize: see README default blob (live dump not captured)
