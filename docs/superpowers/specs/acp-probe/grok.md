# grok ACP probe

- Spawn: `{home}/bin/grok agent stdio`
- RPC extras: `_x.ai/*` on this child only
- Session disk: `~/.grok/sessions/<id>/` including `updates.jsonl`
- Initialize: see README default blob (live dump not captured)
